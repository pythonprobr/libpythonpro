sudo: false
language: python
python:
  - 3.9
install:
  - pip install -q pipenv -r requirements-dev.txt codecov
  - pipenv sync --dev
script:
  - flake8
  - pytest libpythonpro --cov=libpythonpro
after_successpyt:
  - codecov